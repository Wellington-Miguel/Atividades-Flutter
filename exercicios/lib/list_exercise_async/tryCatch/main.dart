import 'package:exercicio/list_exercise_async/tryCatch/tryCache.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'Try cache',
      home: const TryCache(),
      theme: ThemeData(primarySwatch: Colors.indigo),
    ),
  );
}
